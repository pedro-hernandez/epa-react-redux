// src/js/reducers/index.js

const initialState = {
    zip: 0,
    uvIndex: 0,
    uvAlert: 0
};

function rootReducer(state = initialState, action) {
    return state;
};

export default rootReducer;